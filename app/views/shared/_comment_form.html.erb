<%= form_with model: @comment, url: user_post_comments_path(params[:user_id], params[:id]), scope: :comment do |form| %>
  <%= form.text_field :text, placeholder: 'Add your comment...' %>
  <%= form.hidden_field :post_id, value: @post.id %>
  <%= form.submit 'Send' %>
<% end %>