<%= form_with model: @like, url: user_post_likes_path(params[:user_id], params[:id]), scope: :new_like do |form| %>
  <%= form.hidden_field :post_id, value: @post.id %>
  <%= form.submit 'Like!', class: 'btn' %>
<% end %>
